import ContactClient from "./ContactClient";

export const metadata = {
  title: "Contact — Monecuer Inc.",
  description:
    "Reach Monecuer Inc. for collaborations, support, or project inquiries — via email or WhatsApp.",
};

export default function Page() {
  return <ContactClient />;
}
